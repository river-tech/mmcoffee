export enum ERoomStatus {
    AVAILABLE = "AVAILABLE",
    BOOKED = "BOOKED",
}

export enum ERoomType {
    DELUXE = "DELUXE",
    STANDARD = "STANDARD",  
}

export enum EHotelStatus {
    PENDING = "PENDING",
    APPROVED = "APPROVED",
    REJECTED = "REJECTED",
}

export enum EItemtype{
    FLIGHT = "FLIGHT",
  HOTEL = "HOTEL",
}